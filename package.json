{
  "name": "instagram-dm-auto-reply",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "db:migrate:local": "wrangler d1 migrations apply instagram-dm-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply instagram-dm-db --remote",
    "deploy:check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  },
  "engines": {
    "node": ">=20"
  }
}
